export interface AppBranding {
  appName: string;
  seoDescription: string;
  baseUrl: string;
  ogImage: string;
}

export interface AppColors {
  primary: string;
  primaryDark: string;
  primaryLight: string;
  secondary: string;
  accent: string;
  success: string;
  warning: string;
  danger: string;
  darkNav: string;
  textMain: string;
  textMuted: string;
  bgLight: string;
}

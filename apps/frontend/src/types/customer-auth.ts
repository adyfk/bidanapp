export interface CustomerAuthSessionState {
  city?: string;
  consumerId: string;
  displayName: string;
  expiresAt?: string;
  isAuthenticated: boolean;
  lastLoginAt?: string;
  phone: string;
  registeredAt?: string;
}

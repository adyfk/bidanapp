pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,cjs,mjs,jsx,ts,tsx,json,jsonc,md}"
      run: npx biome check --write --no-errors-on-unmatched {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}
